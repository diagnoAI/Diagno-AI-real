import numpy as np

def classify_stone(image_array):
    # Placeholder: Replace with actual model inference
    # Returns True if stone is detected, False otherwise
    return np.random.choice([True, False])